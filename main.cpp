#include "header.h"

#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main()
{
    vector<int> arr = {1, 2, 3, 4, 5};

    cout << "showVector() " << showVector(arr) << endl;

    coutVector(arr);
}